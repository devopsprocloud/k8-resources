apiVersion: v1
kind: Pod
metadata:
  name: affinity-pod1
  labels: 
    name: affinity-pod1
spec:
# list of containers
  containers:
  - name: affinity-pod
    image: nginx
    ports:
    - containerPort: 80
  
--- 

